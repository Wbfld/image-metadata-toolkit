/** Lightweight format-detection entry point with no parser imports. */
export { detectFormat } from "./detect-format.js";
export type { FormatDetection, ImageFormat, ImageMimeType } from "./types.js";
